# Nixpacks-only (if you use the Nixpacks builder). Railway’s default Railpack ignores this file;
# use railway.json deploy.startCommand for collectstatic + gunicorn.
[phases.build]
cmds = ["python manage.py collectstatic --noinput"]
