{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "/bin/sh -c \"python manage.py migrate --noinput && python manage.py collectstatic --noinput && exec gunicorn swipepoint.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --threads 4\""
  }
}
